;;; commentary: perspective settings

;;; code
(use-package perspective
  :ensure t
  :config
  (persp-mode))

;; (use-package persp-projectile
;;   :ensure t
;;   :config
;;   (require 'persp-projectile))

(message "%s" "loaded perspective settings.")
